{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make -j7",
            "type": "shell",
            "command": "make",
            "args": [
                "-j7"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
